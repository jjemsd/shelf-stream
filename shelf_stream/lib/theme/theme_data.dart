import 'package:flutter/material.dart';

class ShelfStreamTheme {
  static ThemeData get lightTheme {
    return ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.green.shade700));
  }
}
